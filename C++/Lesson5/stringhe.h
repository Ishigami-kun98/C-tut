#ifndef STRINGHE_H
#define STRINGHE_H

//Scriviamo le funzioni qui dentro
//Se non dobbiamo cambiare il valore del punt usare const per sicurezza
void stampa_stringa(const char *str);

int lunghezza_Stringa(const char *str);

void inverti_stringa(char * str);

#endif
